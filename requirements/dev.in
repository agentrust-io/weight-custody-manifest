# Runtime dependencies plus the [dev] extra, mirroring python/pyproject.toml.
# Installed with --require-hashes before the SDK goes in with --no-deps, because
# pip cannot hash-pin an editable install in the same invocation. Covers the
# 3.11 to 3.13 test matrix. Compile with:
#   uv pip compile requirements/dev.in --generate-hashes --universal \
#     --python-version 3.11 -o requirements/dev.txt
pydantic>=2.7,<3
cryptography>=50,<51
pytest>=8
pytest-cov>=5
mypy>=1.10
bandit[toml]>=1.7
pip-audit>=2.7
fastapi>=0.111,<1
httpx>=0.27
uvicorn>=0.29
model-signing>=1.1,<2
jsonschema>=4.21
