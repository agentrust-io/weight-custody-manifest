# composed-software.yml: WCM runtime + [dev], plus the base dependencies of the
# cmcp and ca2a snapshots that job checks out (cmcp-runtime at 2cdb168c,
# ca2a-runtime at fc22c644). Those two and the SDK go in afterwards with
# --no-deps. Update this file when either pinned ref moves. Compile with:
#   uv pip compile requirements/composed.in --generate-hashes --universal \
#     --python-version 3.12 -o requirements/composed.txt
# WCM
pydantic>=2.7,<3
cryptography>=50,<51
pytest>=8
pytest-cov>=5
mypy>=1.10
bandit[toml]>=1.7
pip-audit>=2.7
fastapi>=0.111,<1
httpx>=0.27
uvicorn>=0.29
model-signing>=1.1,<2
jsonschema>=4.21
# cmcp-runtime 0.5.0 [project.dependencies]
agentrust-trace>=0.5
agent-manifest>=0.12.0
pyyaml>=6.0
anyio>=4.0
starlette>=0.37
click>=8.1
cedarpy>=4.0
rfc8785>=0.1.4
# ca2a-runtime 0.2.1 [project.dependencies]
cedarpy==4.8.7
